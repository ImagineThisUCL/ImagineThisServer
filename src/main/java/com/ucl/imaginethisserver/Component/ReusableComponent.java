package com.ucl.imaginethisserver.Component;

/**
 * Function used store representation of each reusable code,
 * so that they can be referred to and used easily later.
 */
public enum ReusableComponent {
    P, BUTTON, INPUTFIELD, SLIDER, IMAGE_BUTTON, GOOGLE_MAP, SWITCH, DROPDOWN;
}
